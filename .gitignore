/.claude/

# Bí mật — `.env.example` là bản mẫu và ĐƯỢC commit; `.env` là bản thật và không bao giờ commit.
.env
.env.*
!.env.example

# Build & deps
node_modules/
.next/
out/
coverage/
test-results/
playwright-report/
*.tsbuildinfo
next-env.d.ts
